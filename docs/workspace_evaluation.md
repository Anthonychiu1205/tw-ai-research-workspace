# Workspace Evaluation

v0.6 adds deterministic workspace evaluation checks.

Run:
- `node scripts/evaluate-workspace.mjs`

Output:
- `artifacts/workspace-evaluation.json`

Categories:
- scenarios
- chat stream events
- tools
- artifacts/share bundle
- safety wording

The suite is mock-first and does not require real backend/API keys.
